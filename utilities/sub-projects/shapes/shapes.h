#ifndef shapes_H
#define shapes_H
        void draw_rectangle(int x, int y);
        void draw_invert(int x, int y);
#define null 0
        
struct rectangle {
       int width;
       int height;
};

struct {
       int width;
       int height;       
} *p_rect;



#endif
